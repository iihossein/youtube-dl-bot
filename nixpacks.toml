[phases.setup]
nixPkgs = ["...", "ffmpeg", "nodejs", "git"]

[phases.install]
cmds = [
  "docker pull brainicism/bgutil-ytdlp-pot-provider:latest",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo 'Build complete'"
]